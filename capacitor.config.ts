import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplevendor.app',
  appName: '\u001asimpleVendorApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
